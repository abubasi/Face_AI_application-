# Flask for building the web application
Flask==2.2.2

# OpenCV for image processing and face detection
opencv-python==4.5.5.64

# Numpy for numerical operations (required for OpenCV and machine learning models)
numpy==1.23.1

# Gunicorn for running the application in a production environment
gunicorn==20.1.0

# python-magic for file type detection (can be omitted if not needed)
python-magic==0.4.18

# Flask-Uploads for handling file uploads in Flask applications (optional)
Flask-Uploads==0.2.1

# Pillow for image processing (optional, depending on your app's needs)
Pillow==9.2.0

# Flask-Cors: Use a stable version compatible with your Python version
Flask-Cors==4.0.2
